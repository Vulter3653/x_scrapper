name: Run Zero-Shot Sentiment

on:
  workflow_dispatch:
    inputs:
      target_user:
        description: 'X screen name whose data/<account>/posts.json should be analyzed'
        required: true
        default: 'Wendys'
      analysis_max_posts:
        description: '0 means analyze all posts; use a smaller number for faster test runs'
        required: true
        default: '0'
      sentiment_labels:
        description: 'Comma-separated zero-shot labels'
        required: true
        default: 'positive,neutral,negative'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  sentiment:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: 1. Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 2. Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 3. Install dependencies
        shell: bash
        run: |
          set -euxo pipefail
          python -m pip install --upgrade pip
          pip install -r requirements-sentiment.txt

      - name: 4. Run zero-shot sentiment analysis
        shell: bash
        env:
          TARGET_USER: ${{ inputs.target_user }}
          ANALYSIS_MAX_POSTS: ${{ inputs.analysis_max_posts }}
          SENTIMENT_LABELS: ${{ inputs.sentiment_labels }}
        run: |
          set -o pipefail
          python analyze_posts.py --task sentiment 2>&1 | tee sentiment_analysis.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            tail -n 60 sentiment_analysis.log
            last_error=$(tail -n 1 sentiment_analysis.log | sed 's/%/%25/g; s/\r/%0D/g; s/\n/%0A/g')
            echo "::error title=Sentiment failed::$last_error"
            exit "$status"
          fi
          rm -f sentiment_analysis.log

      - name: 5. Sync dashboard data
        shell: bash
        run: |
          set -euxo pipefail
          python sync_dashboard_data.py

      - name: 6. Inspect changed files
        shell: bash
        run: |
          set -euxo pipefail
          git status --short

      - name: 7. Commit and push analysis results
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

          if [ -z "$(git status --porcelain)" ]; then
            echo 'No changes to commit.'
            exit 0
          fi

          git add -A
          git commit -m "Update analysis results for ${{ inputs.target_user }}"
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          for attempt in 1 2 3; do
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push failed on attempt ${attempt}; rebasing onto origin/main and retrying."
            git fetch origin main
            git rebase origin/main
          done

          echo "Failed to push after retries."
          exit 1
